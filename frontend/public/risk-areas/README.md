# JWLA-034 Current Reference Map + Optional Version Comparison

The GeoJSON files in this directory are generated by:

```bash
cd backend
node generate_jwla034_reference.mjs
```

Do not edit the generated GeoJSON by hand.

## Source and scope

- Circular: `JWLA-034`
- Published: `2026-07-29`
- Official PDF: <https://lmalloyds.com/wp-content/uploads/2025/06/JWLA-034-Saudi-Arabia.pdf>
- Official PDF SHA-256: `125e507bbd187051315bdf80ae30583bc92ec9ad2d280d02fac2d10a019d03b9`
- Geometry inputs: checked-in Marine Regions IHO/EEZ reference data and geo-countries/Natural Earth-derived boundaries.

`jwla-034-reference.geojson` preserves the informational JWLA-034 reconstruction. The UI renders its four `defined-waters` features as the default current view. The JWLA-033-era/backend boundaries and `jwla-034-amendments.geojson` northward Red Sea delta remain optional comparison layers; display controls do not change backend alert rules.

`jwla-034-installations.geojson` is a dedicated lazy-loaded subset for the Guyana and Venezuela offshore-installation context controls. It avoids downloading the full reference asset when those controls are unused, remains reference-only and is rendered without area fill; it does not convert EEZ transit into an alert event.

These generated files are informational reference layers. Every feature carries `contractAlertEligible: false`. They must not be connected to the backend contract geofence without a separate versioned contract-rule design and approval.

## Known limitations

- Egyptian territorial waters are approximated by clipping a 12 NM buffer around the checked-in Egypt boundary; authoritative territorial-sea geometry is required for contract alerts.
- Cabo Delgado is a coastline-buffer reference constrained by the circular's published coordinate limits.
- Guyana and Venezuela EEZ shapes provide offshore-installation context only; simple EEZ transit is not a JWC entry event.
- Named-country geometries are land outlines. JWLA wording generally concerns ports and coastal waters up to 12 NM unless varied.
- The data is not for navigation or automatic coverage determination.
